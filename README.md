# Reverse Marketplace — Next.js Starter

Минимальное Next.js приложение (App Router) для деплоя на Vercel.

## Скрипты
- `npm run dev` — локально
- `npm run build && npm start` — прод

После деплоя открой `/api/health` — должен вернуть `{ ok: true }`.
